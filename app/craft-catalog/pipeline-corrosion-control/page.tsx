import { redirect } from "next/navigation";

// This route was a template artifact — redirects to the services page.
export default function PipelineCorrosionControlPage() {
  redirect("/craft-catalog");
}
